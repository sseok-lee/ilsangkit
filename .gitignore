# Environment variables (NEVER commit secrets!)
.env
.env.*
!.env.example

# Dependencies
node_modules/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Claude Code
.claude/
.mcp.json
.omc/

# Playwright MCP screenshots
.playwright-mcp/

# Shrimp Task Manager
.shrimp_data/

# Planning & Design (local only)
design/
design_backup/
docs_backup/
specs_backup/

# Uploaded assets (images etc)
assets/

# Temp data files
backend/temp/

# HIRA xlsx data files (large, download separately)
backend/data/*.xlsx
backend/data/*.xls
backend/data/*.zip

# Prisma 데이터 소스 — CSV만 커밋, xlsx 원본은 로컬 변환용
backend/prisma/data/*.xlsx
backend/prisma/data/*.xls

# HIRA 자동 다운로드 원본(분기 62MB) — 커밋 금지
backend/prisma/data/extra_hospital_latest/

# Misc
nul
*.log

아카이브.zip
specs/
docs/
lighthouse-reports/
lighthouse-results/
lighthouse-results-*/

# Screenshots / mockups at repo root (debug/design refs, local only)
/*.png
/*.html

# One-off probe/debug scripts
backend/probe_*.ts
frontend/probe_*.ts

# superpowers local working dir (per-developer, not committed)
.superpowers/

# Local audit reports
/GEO-AUDIT-REPORT.md

# Agent workspace artifacts
_workspace*/

# Planning docs (one-off, already applied)
/PRD_*.md
/SEO_*.md
/TASKS_*.md

# Dev-only pages & docs
frontend/pages/msw-demo.vue
frontend/docs/

# Root package files (workspaces not used)
/package.json
/package-lock.json

# Impeccable skill local context (design brief cache)
.impeccable.md
.impeccable/

# Lighthouse CI local results
frontend/.lighthouseci/
.gstack/
